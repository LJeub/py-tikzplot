from setuptools import setup

setup(
    name='tikzplot',
    description='Generate pgfplots code',
    use_scm_version=True,
    setup_requires=['setuptools_scm'],
    author='Lucas G. S. Jeub',
    python_requires='>=3',
    install_requires=[
    ],
    py_modules=['tikzplot'],
)
